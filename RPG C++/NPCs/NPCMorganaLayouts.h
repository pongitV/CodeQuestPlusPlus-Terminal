#pragma once

#include <vector>
#include <string>

class NPCMorganaLayouts {
public:
    static std::vector<std::string> obterArteMorgana();
    static std::vector<std::string> obterArteCaldeirao();
};

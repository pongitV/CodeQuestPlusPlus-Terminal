#pragma once

#include <vector>
#include <string>

class NPCBjornLayouts {
public:
    static std::vector<std::string> obterArteBjorn();
    static std::vector<std::string> obterArteBigorna();
};
